import React from 'react'
import Logo from '../images/logo.png'

const Header = () => {
    return (
        <div>
            <img src={Logo} alt="logo"/>
        </div>
    )
}

export default Header
